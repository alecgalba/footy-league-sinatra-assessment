<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Footy League Database</title>
  </head>

  <body>
    <nav>
      <ul>
        <% if logged_in? %>
          <a href= '/teams'>See All Teams</a>
          <a href= '/leagues'>See All Leagues</a>
          <a href= '/logout'>Sign Out</a>
        <% else %>
          <a href= '/login'>Login to Your Player Profile</a> -----------
          <a href= '/signup'>Not A Member Yet? Sign Up with Footy League</a>
        <% end %>
      </ul>
    </nav>
   <%= yield %>
  </body>
</html>
